FAIL (real bug): recorded Sports+Drama; replayed categories=Kids; runner clicked SPORTS (recorded checkbox), not Kids. set_selection_equality_failed: expected ['kids'], got ['sports']. ROOT CAUSE: direct-strategy (client-side, no search) set_selection relies on checkbox_template_fp.templates['test_id'] to retarget '...-checkbox-{item}', but the annotator's template-derivation pass (_derive_fingerprint_templates) never recurses into the nested set_selection.checkbox_template_fp, so templates={} and the runner falls back to the literal recorded test_id (multiselect-categories-checkbox-sports). Search-strategy pickers (country) are unaffected because they reach by typed label + visible-row match.
