# E2E teach→annotate→replay matrix — 2026-05-28

Each scenario was recorded through the **real** `TeachRecorder` over CDP
(Chrome v148, operator-sim actions via `dispatch_event`/`fill`), annotated
with `pilot annotate --auto`, then replayed through the **real**
`SkillRunner` with a value DIFFERENT from what was recorded, and the result
verified live via CDP eval and/or the backend API. Fresh draft assets were
assigned per scenario to avoid collisions (catalog seeds 9 drafts; the
backend is a Vite middleware — restart reseeds). The destructive workflow
scenario ran last on its own asset.

Artifacts (record/annotate/run-skill stdout, skill JSON, VERDICT) under
`tmp/test-round-2026-05-28-e2e/<NN>-<name>/`.

## Matrix

| # | Scenario | Asset(s) | Recorded value | Replayed value | Runner result | Verified outcome | Verdict |
|---|----------|----------|----------------|----------------|---------------|------------------|---------|
| 1 | title_edit | A-9001 | "Recorded Title" | `input_asset_title="Replayed Title"` | both steps L1 exact, Saved toast | backend `A-9001.title=="Replayed Title"`, status draft | **PASS** |
| 2 | native_select_region | A-9002 | Region=AMER (Americas) | `select_region=APAC` | step L1 exact | live `select-region.value==APAC` (Asia Pacific) | **PASS** |
| 3 | cascading | A-1001 | APAC→India→Hindi(hi-IN) | `select_region=APAC,select_market=IN,select_language=ta-IN` (Tamil leaf) | 4 steps L1 exact | live selects APAC/IN/ta-IN; depends_on waits held | **PASS** |
| 4 | country_multiselect_search | A-1002 | Argentina | `country=Brazil` | both steps L1 exact | live chip `['Brazil']` (replace mode) | **PASS** |
| 5 | categories_multiselect | B-2001 | Sports + Drama | `categories=Kids` | step **fail** (`set_selection_equality_failed: expected ['kids'], got ['sports']`) | runner clicked Sports, not Kids | **FAIL** |
| 6 | accordion_toggle | B-2002 | open (aria-expanded false→true) | replay (no param) | **fail→PASS after fix** | post-fix: live `aria-expanded==true`, panel revealed | **PASS (after fix)** |
| 7 | workflow_submit (destructive) | C-3001 → D-4001/D-4002 | Submit-for-review (C-3001 draft→in_review) | replay on fresh draft | gate **blocked** (CLI); `target_disabled` (auto-approve); **transition fires when button enabled** | D-4002 draft→in_review (verified) | **PARTIAL** |

## Proven runner action-paths (end-to-end through teach→replay over CDP)

- **fill + `_robust_click` + real backend write** (title_edit) — incl. the
  ~2-4s save latency, gated on the Saved-toast / backend signal.
- **`select_option` on native `<select>`** (region) — enum_value codec,
  label/code resolution.
- **`depends_on` cascading selects with baselined waits** (region→market→
  language) — the runner waits each dependent list to repopulate
  (`/api/markets` ~2s, `/api/languages` ~1.5s) before the next pick.
- **`set_selection` server-search multi-select (search strategy)** with
  label-aware reach + label-aware final-equality (country) — replace mode,
  different target verified by chip.
- **destructive transition + human gate + `_robust_click`** (workflow) —
  the gate correctly BLOCKS without approval; with approval and the target
  enabled, the transition fires and the status pill flips (verified).

## Not proven / failed

- **`set_selection` client-side multi-select (direct strategy)** —
  cannot retarget to a different item set on replay (scenario 5).
- **`toggle_state` desired-state idempotency** — not exercised; the
  accordion replays as a plain click (grabber didn't capture
  `target_state_after`), so start-state-independent open is unproven
  (scenario 6 note).

---

## FAIL / PARTIAL root causes

### Scenario 5 — categories_multiselect (FAIL, real bug, NOT fixed — larger change)

Recorded Sports+Drama; replayed `categories=Kids`. The runner ended with
`['sports']`, not `['kids']`, and failed the final equality assertion.

**Root cause.** For a client-side (no server search) multi-select the
annotator sets `select_strategy="direct"`. At replay the runner reaches the
target checkbox via
`_locate_via_template(spec.checkbox_template_fp, {"item": item})`, which
only substitutes when `checkbox_template_fp.templates` carries a
`{item}`-style template (e.g. `test_id="multiselect-categories-checkbox-{item}"`).
That template is **never derived**: `_derive_fingerprint_templates` iterates
only `skill.steps[*].fingerprint` and never recurses into the nested
`set_selection.checkbox_template_fp`; and even if it did, the param's
recorded value bound to the step is the boolean checkbox value `"true"`,
not the item id, so there is no `(param, "sports")` pair to substring-match.
With `templates={}` the runner uses the literal recorded fingerprint
(`...checkbox-sports`) for every item → it always clicks Sports.

Search-strategy pickers (country, scenario 4) are unaffected because they
reach by typing the label and matching the visible row, bypassing the
id-templated checkbox entirely.

**Why not fixed here.** The correct fix (derive a `{item}` template on the
nested `checkbox_template_fp` from the checkbox testid pattern, and have the
runner resolve each item id through it) is a non-trivial annotator+runner
change with its own test surface — not a small, safe one-liner. Flagged as
a design item rather than guessed at.

### Scenario 6 — accordion_toggle (was FAIL, now PASS — fixed, commit `dbc57aa`)

Recorded accordion open (aria-expanded false→true). The annotator
misclassified the toggle as a degenerate `set_selection` (empty
`known_options`, `checkbox_template_fp=None`), so replay failed with
`set_selection: no checkbox_template_fp`.

**Root cause.** `_multiselect_prefix()` matched any testid ending in
`-toggle` (regex `-toggle$`), so `accordion-advanced-toggle` was treated as
a multiselect picker `accordion-advanced`. `_detect_set_selection_clusters`
then claimed the toggle click + the following observed `dom_mutation` (the
aria-expanded flip), and the observed event inflated the cluster past the
`len <= 1` "lone toggle" guard, pre-empting `_detect_toggle_state_clusters`.

**Fix (committed).** A `set_selection` cluster now requires at least one
checkbox/item selection event before it is consumed; a bare `-toggle` click
followed only by observed events falls through. Re-annotated → the step is
a plain replayable `click`; replay opens the accordion (verified
`aria-expanded==true`, panel present). Regression test:
`tests/agent/test_set_selection_wi19.py::test_detector_does_not_claim_accordion_toggle`.

**Residual note (not a bug, documented).** It re-annotates to a plain
`click`, not a `toggle_state`, because the grabber did not capture
`target_state_after` for this click (`_detect_toggle_state_clusters`
requires it to know the desired end state). The plain click correctly opens
a closed accordion but is not start-state-idempotent. Capturing
`target_state_after` on toggle clicks is a separate grabber enhancement.

### Scenario 7 — workflow_submit (PARTIAL, no bug — runner safely strict)

Recorded Submit-for-review (C-3001 draft→in_review). Three findings:

1. **Gate works.** Vanilla CLI replay correctly BLOCKED the destructive
   transition at the human gate (no approval in a non-interactive run).
2. **WI-43 works.** With auto-approve, the runner refused the click with
   `error_kind=target_disabled` ("no readiness signal declared") because the
   Submit button is `disabled` during the post-navigate asset GET and the
   annotator emitted no asset-load readiness signal on the navigate step.
   This is the runner being safely strict, not an action-path defect.
3. **Destructive path proven.** With a real readiness pre-wait (button
   enabled) + auto-approve, `_robust_click` fired and D-4002 transitioned
   draft→in_review (verified via the status pill).

**Design gap (documented, not fixed — needs an annotator decision).**
`annotate` should emit a readiness / post-condition wait on a navigate step
that precedes a click on a control gated by an async load, so vanilla CLI
replay doesn't race the GET. Marked PARTIAL because the destructive
action-path itself is proven but the out-of-the-box CLI replay does not
complete without manual readiness/approval.

## Overall verdict

**Proven end-to-end (teach→annotate→replay over CDP, different value
verified):** native-input fill + real save write, native `select_option`,
`depends_on` cascading selects with baselined waits, server-search
multi-select (`set_selection` search strategy, replace mode, label-aware
equality), and the destructive transition path (gate + `_robust_click` +
state flip).

**Not yet proven / outstanding:**
- Client-side `set_selection` (direct strategy) cannot retarget a different
  item set on replay — real bug, documented (scenario 5), needs a
  template-derivation change.
- `toggle_state` desired-state idempotency is unproven (grabber doesn't
  capture `target_state_after` on the accordion click); the accordion
  replays as a plain click after the fix.
- Destructive CLI replay needs a navigate-readiness signal to avoid racing
  an async load (scenario 7) — annotator design item.

5 of 7 scenarios PASS (one after a fix), 1 PARTIAL, 1 FAIL. Test suite:
**519 passed** (`pytest tests/ --ignore=tests/agent/test_groq_live.py`),
518 baseline + 1 new regression test. One bug fixed and committed
(`dbc57aa`). Nothing pushed.
